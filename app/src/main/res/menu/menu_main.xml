<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_new_game"
        android:title="@string/clear_and_start_new_game"
        android:icon="@xml/refresh"
        android:orderInCategory="1"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/action_clear_winners"
        android:title="@string/clear_glory_list"
        android:icon="@xml/list_remove"
        android:orderInCategory="2"
        app:showAsAction="ifRoom" />
    <item android:id="@+id/action_logout"
        android:title="@string/logout"
        android:icon="@xml/logout"
        android:orderInCategory="2"
        app:showAsAction="ifRoom" />
</menu>